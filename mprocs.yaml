procs:
  builder:
    shell: pnpm build && ./build-apps.ts && curl -X DELETE 'http://192.168.1.31:8081/tabletos.html' && curl -T dist/index.html 'http://192.168.1.31:8081/tabletos.html' && deploy_tabletos_apps
  dev:
    shell: pnpm vite dev
  #lazygit:
  #  shell: lazygit
  tabletos-server:
    shell: cd server && pnpm run dev
