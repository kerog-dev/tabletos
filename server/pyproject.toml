[project]
name = "tabletos-server"
version = "0.1.0"
description = "The server for tabletos"
requires-python = ">=3.14"
dependencies = [
    "httpx[http2]>=0.28.1",
    "starlette>=1.3.1",
    "uvicorn[standard]>=0.49.0",
]
